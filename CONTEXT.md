# Yuxi

智能知识库与知识图谱智能体开发平台。

## Language

**短信认证模式 (SMS Mode)**：
通过环境变量 `SMS_MODE` 切换验证码服务的运行模式。`log` 为自生成模式（开发环境），`dypnsapi` 为阿里云号码认证托管模式（生产环境）。
_Avoid_: SMS_PROVIDER（已废弃，由 SMS_MODE 替代）

**手机验证码登录 (SMS Verification Code Login)**：
用户通过手机号获取一次性验证码完成身份认证的登录方式。不依赖密码。
_Avoid_: 短信登录

**自生成模式 (Self-Generated Mode)**：
应用端自行生成 6 位验证码，存储于 Redis（TTL 300s），用户提交后由应用端比对验证。仅限 `SMS_MODE=log`。
验证码不实际发送，仅在日志中输出。手机号发送冷却 + 失败重试限制均由应用端管理。

**托管模式 (Managed Mode)**：
验证码由阿里云号码认证服务生成（6 位数字）、发送、核验，应用端不持有验证码、不使用 Redis 存储。仅限 `SMS_MODE=dypnsapi`。
手机号发送冷却和失败重试由阿里云端控制（Interval、ValidTime 参数）。

**手机号自动注册 (Phone Auto-Registration)**：
验证码验证通过后，若手机号未在系统中注册，则自动创建用户账户（role=user，username=手机号，密码为随机哈希）。
两种模式下行为一致。

**SMS Provider**：
封装完整的验证码生命周期（发送 + 验证）的策略抽象。自生成模式下为 LogSmsProvider（Redis 存储 + 自生成验证码），托管模式下为 DypnsapiProvider（阿里云 API 调用）。

## Relationships

- 一个 **手机验证码登录** 可能触发一次 **手机号自动注册**（手机号不存在时）
- **手机号自动注册** 创建的用户不可通过密码登录（密码为随机 Argon2 哈希）
- **自生成模式** 下验证码仅存在于 Redis，开发环境可通过 `/api/dev/verification-code` 查询
- **托管模式** 下验证码由阿里云持有，应用端无感知，调试端点不可用
- **IP 限流** 在两种模式下均由应用端执行（安全边界），手机号冷却和重试限制由各 Provider 内部处理

## Example dialogue

> **Dev:** "已有密码的用户使用验证码登录后，密码还能继续用吗？"
> **Domain expert:** "能。验证码登录和密码登录是两套并行的认证通道，互不影响。"
>
> **Dev:** "如果用户 A 之前被管理员创建了账户（有手机号），他用验证码登录会自动注册一个新号吗？"
> **Domain expert:** "不会。系统先按手机号查找，查到已有用户直接登录，查不到才自动注册。"
>
> **Dev:** "从 log 模式切换到 dypnsapi 模式，需要改前端吗？"
> **Domain expert:** "不需要。前端调用 `/api/auth/sms/send` 和 `/api/auth/sms/login` 不变，后端 Provider 切换对前端透明。"

## Flagged ambiguities

- "注册" 被用来指两种行为：管理员在后台创建用户、用户通过验证码自动注册。两者创建的用户权限不同（管理员可指定角色/部门，自动注册固定 user/默认部门），但逻辑上是一个 **手机号自动注册** 概念。
- "发送验证码" 在自生成模式下验证码实际未发送（仅在日志输出），在托管模式下由阿里云真实发送。对外接口和用户体感一致，但开发和测试行为不同。
